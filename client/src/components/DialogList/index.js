 import React from 'react';
import styles from './DialogList.module.css';

const DialogList = () => {
    return (
        <div className={styles.dialog}>
            (список діалогів буде тут)
        </div>
    );
}

export default DialogList;